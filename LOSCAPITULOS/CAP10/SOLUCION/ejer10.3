#
# Copia un Archivo
#
if [ "$#" -ne 2 ]
then
   echo "USO: mycp from to"
   exit 1
fi

from="$1"
to="$2"
# Verifica la si la entrada existe 
if [ -f "%from" ]
  then
# Verifica si el archivo destino existe
#

if [ -f "$to" ]
then
    echo "$to ya existe ; rescribir ( yes/no ) ? "
    read answer

    if [ "$answer" != yes ]
    then
        echo "La copia no se efectuo "
        exit 0
    fi
fi
#
# copia
#
  cp $from $to

else
  echo "El archivo $from no existe"
fi
